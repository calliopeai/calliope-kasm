# Calliope Loadr - Kasm Workspace
# Builds on calliope-base-kasm which provides Node, CLI, agent CLIs, and shared deps.

ARG CALLIOPE_BASE_TAG="latest"
FROM ghcr.io/calliopeai/calliope-base-kasm:${CALLIOPE_BASE_TAG}

USER root

ENV HOME=/home/kasm-default-profile
ENV STARTUPDIR=/dockerstartup
ENV INST_SCRIPTS=$STARTUPDIR/install
WORKDIR $HOME

######### Customize Container Here ###########

# Version can be overridden at build time
ARG CALLIOPE_VERSION="1.0.2"
ENV CALLIOPE_VERSION=${CALLIOPE_VERSION}

# Install Loadr
COPY src/install_loadr.sh $INST_SCRIPTS/loadr/
RUN chmod +x $INST_SCRIPTS/loadr/install_loadr.sh \
    && bash $INST_SCRIPTS/loadr/install_loadr.sh \
    && rm -rf $INST_SCRIPTS/loadr/

# Override startup script
COPY src/loadr_startup.sh $STARTUPDIR/custom_startup.sh
RUN chmod 755 $STARTUPDIR/custom_startup.sh

######### End Customizations ###########

RUN chown 1000:0 $HOME
RUN $STARTUPDIR/set_user_permission.sh $HOME

ENV HOME=/home/kasm-user
WORKDIR $HOME
RUN mkdir -p $HOME && chown -R 1000:0 $HOME

USER 1000
